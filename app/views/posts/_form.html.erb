


<script>
    function init() {
        window.addEventListener('scroll', function(e){
            var distanceY = window.pageYOffset || document.documentElement.scrollTop,
                shrinkOn = 300,
                header = document.querySelector("header");
            if (distanceY > shrinkOn) {
                classie.add(header,"smaller");
            } else {
                if (classie.has(header,"smaller")) {
                    classie.remove(header,"smaller");
                }
            }
        });
    }
    window.onload = init();
</script>
</head>



<body>




    <div id="content">
        <section>
            <div class="container">
                
<%= form_with(model: post, local: true) do |form| %>
  
  
  <div class="form-group">
    <label for="email"><%= form.label :title %>:</label>
     <%= form.text_field :title, id: :post_title, class: "form-control", id: "email", placeholder: 'Put title here' %>
  </div>

  <div class="form-group">
    <label for="pwd"> <%= form.label :description %>:</label>
    <%= form.text_field :description, id: :post_description, id: 'pwd', class: "form-control", placeholder: 'Put description here' %>
  </div>

  <div class="actions">
    <%= form.submit 'submit', class: "btn btn-success" %>

  </div>




<div class="container">
  
  <table class="table">
    <thead>

    <tr><th >All Posts</th></tr>
      <tr>
        <th>Title</th>
        <th>Description</th>
      
      </tr>
    </thead>
    <tbody>
      <% current_user.posts.each do |post| %>     
      <tr class="danger">
        <td> <%= post.title %></td>
        <td><%= post.description %></td>
        
      </tr>
      
      <% end %>
    </tbody>
  </table>
</div>


<% end %> 
                
            </div>
        </section>
       
       
    </div>






   